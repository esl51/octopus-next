import { NavItem } from '@/types'

const nav: Array<NavItem> = [
  {
    position: -100,
    to: { name: 'dashboard' },
    label: 'dashboard.title',
    icon: 'dashboard',
  },
]

export default nav
